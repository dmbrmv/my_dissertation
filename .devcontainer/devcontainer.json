// devcontainer.json
{
	"name": "abramov_flood",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"runArgs": [
		// Cpu only
		"--ipc=host"
		// pc with GPU
		// "--gpus",
		// "all",
		// "--ipc=host"
		// pc with GPU
		// gpu device
		"--gpus",
		"device=0",
		"--ipc=host",
		// memory
		"--memory",
		"48G",
		// cpus
		"--cpus",
		"8.0"
		
	],
	"mounts": [
		// server
		// "source=/home/anton/hdd/data/great_db,target=/workspaces/geo_data,type=bind,consistency=cached"
		// home
		// "source=/home/dima/Coding/geo_data,target=/workspace/my_dissertation/geo_data,type=bind,consistency=cached"
		// macOS
		// "source=/Users/dmbrmv/Development/geo_data,target=/workspaces/my_dissertation/geo_data,type=bind,consistency=cached"
	]
}